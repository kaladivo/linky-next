/**
 * Shared Prettier config for all Linky workspaces.
 *
 * Consumed via `"prettier": "@linky/config/prettier"` in package.json.
 */
export default {
  semi: true,
  singleQuote: false,
  trailingComma: "all",
  printWidth: 100,
};
