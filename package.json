{
  "name": "linky",
  "private": true,
  "packageManager": "pnpm@9.4.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "test": "turbo test",
    "check": "turbo typecheck lint test",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "prettier": "@linky/config/prettier",
  "devDependencies": {
    "@linky/config": "workspace:*",
    "prettier": "^3.8.4",
    "turbo": "^2.9.18"
  }
}
