/**
 * @linky/push — push service for Nostr inbox notifications.
 *
 * Scaffold stub; the actual service is a full rewrite in a later issue
 * (the PoC Bun service is a behavior reference only).
 */
export const main = (): void => {
  console.log("linky push service: not implemented yet");
};

main();
